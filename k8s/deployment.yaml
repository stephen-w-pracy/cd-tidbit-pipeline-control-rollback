apiVersion: apps/v1
kind: Deployment
metadata:
  name: pipeline-controls-demo
  labels:
    app: pipeline-controls-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pipeline-controls-demo
  template:
    metadata:
      labels:
        app: pipeline-controls-demo
    spec:
      containers:
        - name: web
          image: <+artifacts.primary.image>
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: content
              mountPath: /app/content
      volumes:
        - name: content
          configMap:
            name: pipeline-controls-content
